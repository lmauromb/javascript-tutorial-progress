let upLimit = 90;
let downLimit = 14;
let n = 4;

if (n >= downLimit && n <= upLimit) {
    console.log(`${n} is between ${downLimit} and ${upLimit}`);
} else {
    console.log(`${n} is not between ${downLimit} and ${upLimit}`);    
}
